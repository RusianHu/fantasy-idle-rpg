# 幻境远征 · Fantasy Idle RPG

纯前端、可离线运行的 **2D 像素风挂机放置 RPG**（日式中世纪奇幻 / JRPG 风格）。
沉睡千年的魔王在露西亚大陆苏醒，瘴气蔓延八方——你是公会新晋的独行冒险者，从新手草原一路讨伐至魔王城。

## 运行方式

- **双击直开**：直接用浏览器打开 `index.html` 即可游玩（全部脚本按序 `<script>` 加载，无 ES Modules，file:// 协议可用）。
- **静态服务器**（可选）：`python -m http.server 8080` 后访问 `http://localhost:8080/`。
- **移动端优先**：设计基准 390×844 竖屏；桌面浏览器居中显示手机比例容器。
- 无任何构建步骤、无外部网络依赖（EasyStar.js 0.4.4 MIT 与 Fusion Pixel 字体均已本地固定）。

## 玩法概览

| 系统 | 说明 |
| --- | --- |
| **开场体验** | 标题画面采用半俯视悬崖构图：全景先在半分辨率画布逐像素绘制，再以 2× 最近邻放大；四名队友在近岸营地围火、游侠在同侧河岸警戒上游，完整岩壁明确高台落差；远山曲流在崖口收束并与瀑布同轴，落入带浅滩、暗岸与河中岩石的横向河湾，视线继续跨过巨兽巢穴、不规则混生密林、多重雪山，最终落到山隘间极远的小比例魔王城；启动先展示无遮挡营地观景态，点击进入后才升起公会远征档案，并可随时收起返回观景；空档显示「开始新游戏」，进行中档显示职业头像、等级、区域、游玩时长、路线进度、世界种子与「继续游戏」；确认档案后经传送光柱、符文与像素帘幕进入序章或世界 |
| **职业系统** | DnD 风格五职业（战士/盗贼/法师/牧师/游侠），开局选择、永久生效；数值成长、攻击方式（近战/远程弹道）、技能组、精灵立绘、武器全面区分 |
| **环境视觉** | 程序化多阶树冠大树（摇曳）、草簇风场行波、烘焙花簇/色斑/裂纹/雪地反光、发光体光晕（水晶/烛火/营灯）、林间光柱、道具软阴影、暗角；营地含磨损地垫、刻印、旗帜、补给、铺盖、坐木与炊具；全部可开关、视口剔除（实测满帧） |
| 挂机战斗 | 伪实时制：每个实体按速度决定的独立攻击间隔自动出手；技能按冷却自动释放（受冷却缩减影响）；闪避/吸血/护盾/持续伤害/临时增益全套机制 |
| 平面世界 | 每个新存档以世界种子生成八区不同布局；营地、Boss、主题道路、材质块和装饰均受约束随机，同一存档永久稳定；角色与怪物使用 16px 加权路径自动游走 / 索敌 / 接敌 |
| **自动/手动操控** | 世界舞台常驻双态开关，默认自动挂机；手动时停止游走、索敌和击杀后续敌，支持点地移动、点怪交战及 WASD/方向键，已锁定目标仍按伪实时节奏攻击 |
| **点触交互** | 自动模式下点怪物=临时优先目标（金圈）、点地面=移动指令（绿圈，抵达后恢复 AI）；手动模式下相同操作成为持续玩家指令；点篝火=回营/拔营 |
| 区域推进 | 8 大区域；讨伐进度积满触发 Boss。首杀后以 3 秒非模态路线卡片、传送阵、像素帘幕和营地镜头完成换景，可立即出发或仅取消本次自动旅行 |
| **最终通关** | 路线末区 Boss 首杀触发最终击杀演出与六句逐字后日谈，随后展示通关摘要；可继续最终区域挂机或确认后重开新档 |
| 装备 | 武器/护甲/饰品 3 槽；5 档稀有度（灰绿蓝紫橙）；随机词条、职业/区域感知的综合对比、一键出售、传说分解魔晶石；武器按职业呈现（长剑/短匕/法杖/战锤/长弓） |
| 技能 | 每职业 3 主动 + 3 被动（共 30 个），技能点升级（每级 +1 点）；主动技能 0 点可用基础值，每投入 1 点都有真实提升 |
| **自动养成** | 自动技能加点与智能换装默认开启：逐候选模拟输出/生存/收益后决策；三个装备槽可分别锁定，支持完整手动构筑 |
| 商店 | 金币：药水/装备箱；魔晶石：史诗装备箱/四种永久强化（可叠 10 层） |
| 返回营地 / 休整 | 按距离显示「返回营地」或「传送回营」，配套手绘像素图标；低于自动喝药阈值时按钮闪烁。休息时主界面切换为营地信息牌与休整进度；Boss 战可安全撤离回营并保留一半讨伐进度；坐下后快速恢复 HP、积累「休整」增益（战斗时 +15% 经验 / +10% 掉率） |
| 死亡重整 | 零惩罚：约 6.8 秒完成倒下、灵魂回收、营地落地、篝火恢复和复苏；自动操控复战、手动操控留营。同区第三次普通死亡直接撤往上一区域，Boss 失败仅撤场并保留一半进度 |
| 离线收益 | 关闭页面再回来按公式结算（无时长上限），弹窗确认后入账；休息模式离线则回满 HP + 休整增益积满 |
| 统计成就 | 事件总线驱动的统计页（14 项计数器）+ 18 个累计型成就（奖励金币/魔晶石） |
| 日夜氛围 | 20 分钟一昼夜，晨昏暖橙 / 夜晚深蓝 + 星空月亮；八区专属环境粒子；草丛摇曳、雪地沙地脚印、浅水涟漪、熔岩火星等地形反馈 |
| 国际化 | 简体中文（默认）/ English，设置内即时切换；数字缩写随语言（万/亿 vs K/M/B/T） |

## 职业一览（DnD 核心四职 + 游侠）

| 职业 | 定位 | 攻击方式 | 专属机制 |
| --- | --- | --- | --- |
| 战士 Fighter | 近战坦克 | 近战 | 战吼增益、战斗回血、高 HP/DEF |
| 盗贼 Rogue | 近战爆发 | 近战 | 高速高暴击、闪避、毒素持续伤害 |
| 法师 Wizard | 远程法术 | 远程（法弹） | 最高攻击、冷却缩减、奥术护盾 |
| 牧师 Cleric | 近战续航 | 近战 | 治疗强化（含药水）、吸血、打击回血 |
| 游侠 Ranger | 远程物理 | 远程（箭矢） | 鹰眼增益、寻宝（金币/掉率加成） |

职业选定后**不可更改**（DnD 建卡传统）；换职业 = 重置存档。v1.0 旧存档载入时会提示补选职业，
旧技能点全额退还（等价一次免费洗点）。设计细节见 `PLAN/multiclass-plan.md`。

## 目录结构（引擎与内容分离）

```
index.html            入口（按序加载脚本）
css/style.css         像素 JRPG UI（FF/DQ 式双线边框面板）
assets/fonts/         Fusion Pixel 12px 中文像素字体（woff2）
tech-demos/           技术验证演示页（支持区域/世界种子 URL 参数）
js/
  vendor/             EasyStar.js 0.4.4（MIT，本地固定）
  core/               utils / eventbus / registry / audio(占位) / assets(精灵工厂) / save / loop
  i18n/               i18n 核心 + zh-CN + en 语言包
  data/               纯内容配置：formulas(数值公式集中) / regions / monsters / items / affixes / skills / achievements
  sprites/            像素素材（字符网格+调色板，运行时编译为 canvas，自动描边/镜像/呼吸帧）
  systems/            state / terrain(布局生成) / nav(加权路径) / inventory / automation / combat / world / progression / transitions / ending / offline / shop / meta
  render/             renderer(镜头/视差/合成) / terrain(地表烘焙+贴花) / particles / daynight / effects
  ui/                 hud / 六 Tab 面板 / transitions(短过场层) / ending(结局层) / 弹窗(逐字叙事/离线/确认/Toast)
  main.js             启动引导
```

**扩展方式**：新增一个区域 = 在 `data/regions.js` 注册一条数据（+ 怪物注册 + 语言包 key + 可选精灵），引擎自动生成地图列表、刷怪、掉落与 UI，零引擎改动。所有内容引用一律走稳定字符串 ID；存档遇到已下线 ID 自动优雅降级（装备折算金币、区域回退）。

**区域顺序**：新档用 `world.worldSeed` 确定性排列前 4 个区域，后 4 个区域固定；怪物强度、奖励与推荐等级按本档推进位置计算。顺序写入存档，旧档迁移不会重新洗牌。

## v1.7 高密度区域布局

- `world.worldSeed` 是只读 `uint32`，地图面板显示 8 位十六进制值并可复制；刷新、离线回归和导出/导入不改变布局。
- `Game.terrain.build(region, worldSeed, layoutVersion)` 依次生成关键地标、材质块、主题走廊、装饰/细节、16px 导航代价和低代价出生候选；区域注册对象只保存约束。
- 根种子拆成 `landmarks / terrain / decor / details` 四个子流，调整花草细节不会改变营地、Boss、道路、材质块或大型装饰。
- 地形与大型装饰只提供软代价，不设置不可通行格。雪/沙、水、瘴气、熔岩依次提高移动代价，EasyStar 求解失败时回退直线移动。
- `layoutVersion:2` 按区域密度配置生成 98–135 个装饰、10–14 个材质斑块和 2.0–2.4 倍地表细节；大型丛聚装饰部分沿主路线两侧布置，森林、矿坑和洞窟的实际移动镜头不再稀疏。
- 历史 `layoutVersion:1` 生成路径与固定快照保留；v6 存档升级 v7 时只变化一次到 v2，世界种子和区域顺序不变。密集装饰的软导航代价封顶 `2.8`。
- 八区 QA：`tech-demos/map-effects/map-effects.html?seed=1234ABCD&region=forest`。页面可改 QA 种子，正式游戏不可重掷。

## v1.8 最终通关

- 仅在本档路线末区 Boss 首杀时触发：最终一击停顿与像素消散 → 瘴气退去、晨光重临 → 与序章同交互的六句逐字后日谈 → 通关摘要。
- 演出期间保留 Canvas、相机和氛围渲染，暂停战斗、刷怪、世界时间、增益与统计；关闭页面后从已保存的后日谈句或摘要恢复，不产生暂停期间的离线收益。
- 摘要展示职业等级、累计游玩、总讨伐、Boss 击杀和世界种子。「继续游戏」恢复最终区域并允许重复 Boss；「重开一局」确认后清空存档并重新进入序章与职业选择。
- v7 已通关存档升级后补播后日谈与摘要，不伪造最终击杀镜头；完整结局每档只播放一次。

## v1.9 地图过场与死亡重整

- 自动推进固定为 3 秒路线倒计时、0.45 秒传送收束、0.18 秒遮罩原子换图、0.85 秒营地抵达；「留在本区」与 Esc 只取消本次，不改自动推进设置。手动选图跳过倒计时，仍复用完整换景。
- 自动操控抵达后复战；手动操控或手动首次进入新区时留在营地，重访则保留换图前的战斗/休息状态。世界只在遮罩最深处重建一次。
- 死亡为倒下 0.65 秒、灵魂回收 0.8 秒、营地落地 0.45 秒、分段恢复 4.2 秒、复苏 0.75 秒。恢复段不可快进；关闭特效或减少动态效果时保留时长与信息，移除震屏、残影和密集粒子。
- `Game.transitions` 统一提供 `startRegion/startDeath/cancel/update/isActive/blocksWorld/cameraTarget/settleBeforeSave`；玩家可见换区统一走 `Game.prog.requestRegion(rid,{source})`，`gotoRegion` 仅供遮罩中点、启动和导入等原子操作。
- 新事件为 `region:travelStart`、`region:travelCancelled`、`region:arrived`、`player:reviveStart`、`player:revived`；`region:changed` 仍只在真实世界重建时触发一次。存档保持 v8。

## v1.10 标题存档门厅

- 每次启动都先显示公会「远征档案」，不再让已有角色直接进入世界。当前只开放 `expedition-1` 单槽；UI、回调和存储描述均按稳定槽位 ID 数组渲染，为未来多档保留扩展面。
- 空档主操作为「开始新游戏」；建角草稿为「继续登记」；正式档为「继续游戏」，并显示职业像素头像、等级、驻扎区域、游玩时长、路线进度、世界种子和最后记录时间。
- 正式档保留清晰的双行「开始新游戏」档案页签，副文案提示将覆盖当前档案；存档卡右侧另有 44px 的「X」删除键。两者分别二次确认：前者清档后自动衔接新档进入动画，后者只删除并返回空槽。
- 选择档案前不启动主循环、不自动存档、不结算离线收益、不推进世界时间；离线时长在明确进入后才按原时间戳结算。
- 进入演出由档案卡收束、营火传送光柱、旋转公会符文、星屑与上下像素帘幕组成；摄像机连续缓动，帘幕按像素步进，减少动态效果时保留静态遮罩与相同流程。

## 存档

- localStorage 双槽写入（`firpg_save` + `firpg_save_backup`），主档损坏自动回退备份档。
- 当前产品层为单个逻辑角色槽位 `expedition-1`；这里的“双槽”指同一角色档案的主写入与容灾备份，不是两个可选角色。
- 每 15 秒自动保存 + 关键事件（升级/Boss/穿戴/购买等）即时保存 + 页面隐藏/关闭时将短过场结算到安全状态后保存。
- 存档带版本号，采用「版本号 + 迁移函数数组」流水线；当前 v8。v5 旧档派生稳定世界种子，v6 旧档一次性升级到高密度布局 v2，v7 已通关旧档补记结局状态；迁移均保留原区域顺序与养成进度。
- 导出/导入：Base64 串（末尾附 FNV-1a 校验和，截断/篡改会被拒绝）与 `.json` 文件下载/导入并存；重置需二次确认。
- 检测到存档时间戳在未来（回调系统时间）时离线收益按 0 处理。

## 数值设计说明

**成长方案**：二选一中采用「升级自动成长」——挂机游戏应尽量减少强制打断。升级自动提升全属性、回满 HP 并 +1 技能点；自动技能与换装默认开启，玩家可关闭总开关或锁定单个装备槽，随时切回完整手动决策。

**自动评估**：使用无副作用角色预览统一计算期望输出、对当前评估区域 Boss 的生存时间与经验/金币/掉落收益，并按职业权重取对数综合。永久技能点以最高已解锁区域评估，装备以当前区域评估；智能换装至少提升 0.1% 才替换，同分保留当前装备。

**职业数值形状**（基础值与复利见 `data/classes.js`）：五职业按「DPS × 有效生命 ≈ 恒定功率预算」设计——战士(HP150/复利1.078)坦而稳、盗贼(SPD12.5/暴击12%)快而脆、法师(ATK17/复利1.075)远程爆发、牧师(治疗强化+吸血)续航、游侠(远程+寻宝)均衡；远程职业以更低生存换取接敌前 1~2 次白嫖攻击。离线估算乘以职业爆发系数（战1.20/贼1.30/法1.35/牧1.15/侠1.25）。

**升级曲线**：`expNeed(L) = 28·L·1.16^(L-1)`（指数）。速度/暴击全职业线性微增。

**伤害公式**：`dmg = atk² / (atk + def)`（平滑减伤，无硬墙），±10% 浮动；暴击默认 150% 可成长。攻击间隔 `1.7·10/(5+spd/2)` 秒，速度既加攻速也间接加 DPS。

**区域怪物匹配**：怪物按区域 tier 指数缩放 `≈2.05^(t-1)`，与「玩家等级复利 × 装备加成（约 35%~55% 战力）」的复合成长率对齐；每区预期停留约 8~10 级，形成「入区偏难 → 毕业碾压」的推图节奏。Boss 为普通怪的 HP×11 / ATK×2.0，首杀奖励魔晶石 `15+10·tier`。

**经济闭环**：金币（产出：击杀/出售/离线；消耗：药水/装备箱）；魔晶石（产出：Boss 首杀/成就/分解传说；消耗：史诗装备箱/永久强化，价格 `10·1.6^已购层数`）。

**离线收益**：`kills = 时长 / (单怪击杀耗时 + 2.5s 位移开销) × 0.8 效率`，收益按当前区域普通怪均值折算，装备掉落上限 30 件（超出折金币方向），无离线时长上限。

**防护设计**：药水低于阈值自动使用（默认 30%，可调，8 秒冷却），同一阈值驱动回营按钮警示；无药水靠自然恢复不中断挂机；死亡零惩罚 + 连败 3 次自动回退；Boss 讨伐失败或主动撤离均保留一半进度，且血量不足 60% 时 Boss 暂缓登场，避免「登场即团灭」循环。

**大数字**：全局格式化函数按语言分派（zh：万/亿/万亿；en：K/M/B/T），超过 1e16 转科学计数法。

## 音频（本期占位）

`AudioManager` 已提供完整签名的空实现（`playSfx/playBgm/stopBgm/setMuted`，内部 console.debug 验证触发点），全部触发点已通过事件总线埋好（击杀/暴击/升级/掉落/购买/扎营/Boss/出发/抵达/灵魂回归/复苏）。未来接入时只需注册音频 manifest 并替换内部实现，零逻辑改动。注意移动端自动播放策略：`AudioContext` 需在用户首次交互后初始化/恢复。

## 验证

```powershell
node tests\v1_8.test.js
node tests\v1_9.test.js
node tests\browser-smoke.js
```

第一项对 v1/v2 八区各跑 100 个种子（共 1,600 张布局），覆盖固定快照、路径、迁移与结局；第二项覆盖旅行/重整状态机的取消、抵达策略、大 `dt`、回退、减少动态效果和安全结算；第三项使用本机 Chrome 验证 390×844/桌面、中英文、过场五阶段、触控尺寸、八区性能与既有结局。浏览器用例默认读取 `http://127.0.0.1:4176/`，可用 `FIRPG_URL` 覆盖。

---

v1.10.0 · 纯 HTML/CSS/JS · UTF-8 · 离线可用
